console.log('edit js file')

//where the edit goes
studentdiv = document.getElementById('student-<% @student.id%>')
 
 //shows what was clicked on
 console.log("cliked" + studentdiv.innerText)

 //used jquery to replace TR because they do not like formd inside of them
 $(studentdiv).replaceWith("<%=j (render 'form') %>");